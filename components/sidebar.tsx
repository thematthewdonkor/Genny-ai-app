import { SidebarRoutes } from "./sidebar-routes";
const Sidebar = () => {
  return (
    <div className="border-r h-screen flex-col">
      <SidebarRoutes />
    </div>
  );
};

export default Sidebar;
